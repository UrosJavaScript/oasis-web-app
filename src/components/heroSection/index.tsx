import LeftColumn from "./LeftColumn";
import RightColumn from "./RightColumn";

import "./style.scss";

const HeroSection = () => {
  return (
    <section>
      <LeftColumn />
      <RightColumn />
    </section>
  );
};

export default HeroSection;
